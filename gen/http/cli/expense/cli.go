// Code generated by goa v3.12.4, DO NOT EDIT.
//
// expense HTTP client CLI support package
//
// Command:
// $ goa gen github.com/chaewonkong/budget-planner/design/expense

package cli

import (
	"flag"
	"fmt"
	"net/http"
	"os"

	expensec "github.com/chaewonkong/budget-planner/gen/http/expense/client"
	goahttp "goa.design/goa/v3/http"
	goa "goa.design/goa/v3/pkg"
)

// UsageCommands returns the set of commands and sub-commands using the format
//
//	command (subcommand1|subcommand2|...)
func UsageCommands() string {
	return `expense add-expense
`
}

// UsageExamples produces an example of a valid invocation of the CLI tool.
func UsageExamples() string {
	return os.Args[0] + ` expense add-expense --body '{
      "amount": 0.8157796479103911,
      "category": "Qui quibusdam id veritatis ea.",
      "memo": "Nostrum et provident cupiditate."
   }'` + "\n" +
		""
}

// ParseEndpoint returns the endpoint and payload as specified on the command
// line.
func ParseEndpoint(
	scheme, host string,
	doer goahttp.Doer,
	enc func(*http.Request) goahttp.Encoder,
	dec func(*http.Response) goahttp.Decoder,
	restore bool,
) (goa.Endpoint, any, error) {
	var (
		expenseFlags = flag.NewFlagSet("expense", flag.ContinueOnError)

		expenseAddExpenseFlags    = flag.NewFlagSet("add-expense", flag.ExitOnError)
		expenseAddExpenseBodyFlag = expenseAddExpenseFlags.String("body", "REQUIRED", "")
	)
	expenseFlags.Usage = expenseUsage
	expenseAddExpenseFlags.Usage = expenseAddExpenseUsage

	if err := flag.CommandLine.Parse(os.Args[1:]); err != nil {
		return nil, nil, err
	}

	if flag.NArg() < 2 { // two non flag args are required: SERVICE and ENDPOINT (aka COMMAND)
		return nil, nil, fmt.Errorf("not enough arguments")
	}

	var (
		svcn string
		svcf *flag.FlagSet
	)
	{
		svcn = flag.Arg(0)
		switch svcn {
		case "expense":
			svcf = expenseFlags
		default:
			return nil, nil, fmt.Errorf("unknown service %q", svcn)
		}
	}
	if err := svcf.Parse(flag.Args()[1:]); err != nil {
		return nil, nil, err
	}

	var (
		epn string
		epf *flag.FlagSet
	)
	{
		epn = svcf.Arg(0)
		switch svcn {
		case "expense":
			switch epn {
			case "add-expense":
				epf = expenseAddExpenseFlags

			}

		}
	}
	if epf == nil {
		return nil, nil, fmt.Errorf("unknown %q endpoint %q", svcn, epn)
	}

	// Parse endpoint flags if any
	if svcf.NArg() > 1 {
		if err := epf.Parse(svcf.Args()[1:]); err != nil {
			return nil, nil, err
		}
	}

	var (
		data     any
		endpoint goa.Endpoint
		err      error
	)
	{
		switch svcn {
		case "expense":
			c := expensec.NewClient(scheme, host, doer, enc, dec, restore)
			switch epn {
			case "add-expense":
				endpoint = c.AddExpense()
				data, err = expensec.BuildAddExpensePayload(*expenseAddExpenseBodyFlag)
			}
		}
	}
	if err != nil {
		return nil, nil, err
	}

	return endpoint, data, nil
}

// expenseUsage displays the usage of the expense command and its subcommands.
func expenseUsage() {
	fmt.Fprintf(os.Stderr, `Expense handler
Usage:
    %[1]s [globalflags] expense COMMAND [flags]

COMMAND:
    add-expense: AddExpense implements AddExpense.

Additional help:
    %[1]s expense COMMAND --help
`, os.Args[0])
}
func expenseAddExpenseUsage() {
	fmt.Fprintf(os.Stderr, `%[1]s [flags] expense add-expense -body JSON

AddExpense implements AddExpense.
    -body JSON: 

Example:
    %[1]s expense add-expense --body '{
      "amount": 0.8157796479103911,
      "category": "Qui quibusdam id veritatis ea.",
      "memo": "Nostrum et provident cupiditate."
   }'
`, os.Args[0])
}
